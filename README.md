# 🎮 Coin Rush: OOP Lab 3

**Варіант:** Лабораторна робота №3 (ООП в ігровому двигуні)  
**Мова реалізації:** JavaScript + Phaser 3 Framework + HTML + CSS 
**Запуск:** через Live Server (VS Code) — сторінка `index.html`. **Результати тестів у консолі (F12).**

---

## 💡 Ідея програми

**Coin Rush** — це 2D платформер, розроблений з використанням бібліотеки Phaser 3. Гравець керує персонажем (котиком), стрибає по платформах, збирає монетки та намагається не впасти вниз.

**Основна мета цієї лабораторної роботи** — не стільки розробка геймплею, скільки покращення архітектури проєкту з використанням принципів об'єктно-орієнтованого програмування (ООП), впровадження слабкої зв'язності через систему подій та реалізація модульного тестування.

---

## ⚙️ Технічні та Функціональні можливості

В ході виконання лабораторної роботи було реалізовано та покращено наступні ключові механіки:

### 🔹 ООП та Архітектура

1.  **Поліморфізм ігрових об'єктів (Клас `Coin`)**
    * Базовий клас `Phaser.Physics.Arcade.Sprite` розширено власним класом `Coin`.
    * Реалізовано поліморфний метод `interact()`. Замість стандартної поведінки, монетка має власну логіку взаємодії: при контакті з гравцем вона деактивується, стає невидимою та вимикає своє фізичне тіло.

2.  **Слабка зв'язність через Події (Event-Driven Architecture)**
    * Виправлено критичну архітектурну помилку в класі `Scoreboard`.
    * Раніше `Scoreboard` міг звертатися до неініціалізованих об'єктів сцени, що викликало помилки (`Score: NaN`).
    * Тепер взаємодія реалізована через систему подій Phaser: ігрова сцена випромінює подію `'collectCoin'`, а `Scoreboard` безпечно підписується на неї для оновлення рахунку.

3.  **Менеджмент ресурсів (Singleton патерн)**
    * Використовується статичний клас `AssetsManager` для централізованого зберігання шляхів до асетів (спрайтів, фонів) та ключів анімацій.

### 🔹 Геймплей (Базовий)

4.  **Керування персонажем**
    * Переміщення вліво/вправо та стрибок за допомогою стрілок клавіатури.

5.  **Фізика та колізії**
    * Використання аркадної фізики Phaser для обробки зіткнень з платформами та перекриттів (overlaps) з монетками.

6.  **Інтерфейс користувача (UI)**
    * Динамічне відображення поточного рахунку, який закріплено на екрані (не рухається за камерою).

---

## 🧩 Інтерфейсні зони

Гра складається з кількох сцен (Scene), які є аналогами "вікон" або станів програми у Phaser:

1.  **StartScene**
    * Початковий екран завантаження ресурсів.

2.  **GameScene (Основна гра)**
    * Ігровий світ (платформи, фон, гравець, монети).
    * UI шар (текст рахунку `Scoreboard`).

3.  **GameOverScene**
    * Екран завершення гри при падінні персонажа.

---

## 🕹️ Елементи керування (Inputs)

* `Стрілка ВГОРУ` — Стрибок.
* `Стрілка ВЛІВО` — Рух ліворуч.
* `Стрілка ВПРАВО` — Рух праворуч.
* Також реалізоване керування за допомогою клавіш WASD та пробілу.

---

## 🔁 Обробники подій (Event Handlers)

У проєкті використовуються як вбудовані події фізичного двигуна, так і кастомні архітектурні події:

* **Фізичні події (Phaser Physics):**
    * `collider` (гравець з платформою) — для забезпечення стояння на поверхні.
    * `overlap` (гравець з монеткою) — тригер для виклику методу `coin.interact()`.
* **Кастомні події (Custom Events):**
    * `'collectCoin'` — подія, яку випромінює сцена (`this.events.emit('collectCoin')`), коли гравець підбирає монету.
* **Події сцени:**
    * `Phaser.Scenes.Events.SHUTDOWN` — використовується у `Scoreboard` для відписки від подій при знищенні сцени (уникнення витоку пам'яті).

---

## 📦 Контейнери даних

* **Стан об'єкта `Scoreboard`**
    * `this.score` — числова змінна, що зберігає поточний рахунок гри.
* **Фізичний світ Phaser**
    * Внутрішні структури даних двигуна, що зберігають групи об'єктів (платформи, монети) та їх стан (активний/неактивний, координати).

---

## 🧱 Структура проєкту

```text
lab3.sem1/
│
├── assets/               # Графічні ресурси гри
│   ├── clouds.png        # Зображення для фону з хмарами
│   ├── coin.png          # Спрайт монетки
│   ├── obstacle.png      # Спрайт перешкоди (бочки)
│   ├── platform.png      # Текстура для платформи (трава/земля)
│   └── player_cat.png    # Спрайт персонажа (котика)
│
├── src/                  # Вихідний код гри на JavaScript
│   ├── game_objects/     # Класи ігрових сутностей (реалізація ООП)
│   │   ├── AssetsManager.js # Статичний менеджер шляхів до ресурсів
│   │   ├── Coin.js          # Клас монетки з поліморфним методом interact()
│   │   ├── Collectable.js   # Базовий клас для предметів, що збираються
│   │   ├── Obstacle.js      # Клас перешкоди
│   │   ├── Player.js        # Клас гравця
│   │   └── Scoreboard.js    # Клас таблиці рахунку (слухач подій)
│   │
│   ├── scenes/           # Ігрові сцени (стани гри)
│   │   ├── GameOverScene.js # Сцена завершення гри
│   │   ├── GameScene.js     # Основна ігрова сцена
│   │   ├── SceneManager.js  # Менеджер для керування переходами між сценами
│   │   ├── StartScene.js    # Початкова сцена завантаження
│   │   └── UIScene.js       # Сцена для відображення інтерфейсу (UI) поверх гри
│   │
│   ├── game.js           # Головний файл ініціалізації конфігурації гри Phaser
│   └── main.js           # Точка входу: запуск тестів та створення екземпляра гри
│
├── tests/                # Модульні тести
│   ├── AssetsManager.test.js # Тести для менеджера ресурсів
│   ├── GameObjects.test.js   # Структурні тести ігрових об'єктів
│   ├── Scoreboard.test.js    # Тести логіки таблиці рахунку
│   └── testUtils.js      # Утиліти для тестування (власний тест-раннер)
│
├── .gitignore            # Файл виключень для Git
├── index.html            # Головний HTML файл (точка входу в додаток)
├── README.md             # Основна документація проєкту
├── RETROSPECTIVE.md      # Звіт-ретроспектива використання бібліотеки Phaser 3
└── style.css             # Таблиця стилів для оформлення сторінки
```
## 🧪 Модульне тестування

В рамках лабораторної роботи було розроблено власну систему модульного тестування, яка працює безпосередньо у браузері. Це дозволило перевірити ключову логіку без необхідності розгортання складного середовища тестування (типу Jest/Mocha) з підтримкою canvas.

Тести запускаються автоматично при завантаженні сторінки `index.html` перед стартом гри. **Результати виводяться у консоль розробника (F12).**

### 📁 Структура та інструменти тестування

* **`tests/testUtils.js` (Test Runner):**
    * Реалізовано власні функції `assertNotEqual`, `assertEqual` для перевірки тверджень.
    * Функція `runTest` забезпечує виконання тесту, перехоплення помилок та кольорове форматування виводу в консоль (✅ PASS / ❌ FAIL).

### 🧪 Покриття модулів

#### ✔ `AssetsManager.test.js`
* Перевіряє цілісність статичних даних.
* Підтверджує, що шляхи до критично важливих асетів (спрайт гравця, фон) визначені коректно.

#### ✔ `Scoreboard.test.js` (Тестування з Mock-об'єктами)
* Найскладніший набір тестів. Оскільки `Scoreboard` залежить від сцени Phaser, для його тестування було створено **Mock (імітацію) сцени**.
* Фейкова сцена імітує методи додавання тексту (`add.text`) та систему подій (`events.on`, `events.off`), що дозволяє тестувати логіку `Scoreboard` ізольовано від двигуна.
* Перевіряється: початковий рахунок (0), коректність методу `addScore()` та оновлення тексту.

#### ✔ `GameObjects.test.js` (Структурне тестування)
* Через складність емуляції внутрішніх процесів фізичного двигуна Phaser у простому тестовому середовищі, було застосовано підхід структурного тестування.
* Перевіряється наявність класу `Coin` та коректність визначення поліморфного методу `interact()` у його прототипі.