# 📝 Ретроспектива використання зовнішніх бібліотек (Phaser 3)

**Предмет:** Основи об'єктно-орієнтованого програмування
**Лабораторна робота:** №3

У цій лабораторній роботі для реалізації 2D платформера було використано зовнішній фреймворк **Phaser 3**. Цей звіт підсумовує досвід роботи з бібліотекою, аналізує позитивні та негативні аспекти її використання в контексті вимог ООП та тестування.

---

### 1. Задачі та вибір бібліотеки

**Які конкретні задачі планували вирішувати за допомогою цієї бібліотеки?**
Основна задача полягала у створенні гри, де бібліотека бере на себе роль "двигуна", тобто вирішує низькорівневі технічні питання:
* Рендеринг графіки (WebGL/Canvas) у браузері.
* Керування ігровим циклом (game loop — оновлення стану та перемальовка кадрів).
* Завантаження та кешування ресурсів (зображень, спрайтів).
* Обробка введення користувача (клавіатура).
* Реалізація фізичної взаємодії об'єктів (гравітація, колізії, перекриття).

**Чому було обрано саме цю бібліотеку?**
Phaser 3 є індустріальним стандартом для розробки 2D ігор на JavaScript. Його було обрано через:
* Наявність вбудованого, оптимізованого для платформерів фізичного рушія Arcade Physics.
* Зручну архітектуру на основі сцен (Scene), що дозволяє логічно розділити стани гри (меню, гра, кінець гри).
* Велику екосистему, детальну документацію та велику кількість прикладів.

---

### 2. Початок роботи та документація

**Наскільки просто було почати використовувати бібліотеку?**
Поріг входження виявився невисоким. Можливість підключення бібліотеки через CDN (один тег `<script>`) дозволила почати розробку миттєво, без необхідності налаштування складного середовища збірки (Webpack/Vite), що є великим плюсом для навчального проєкту. Базовий шаблон сцени з методами `preload`, `create`, `update` є інтуїтивно зрозумілим.

**Наскільки корисною була документація?**
Офіційна API-документація Phaser є надзвичайно детальною, але через її обсяг новачкові легко в ній загубитися. Набагато кориснішим ресурсом виявився сайт з офіційними прикладами (Phaser 3 Examples). Аналіз працюючого коду прикладів давав відповіді на питання "як це зробити" швидше, ніж читання сухого опису класів.

---

### 3. Взаємодія з кодом та використання ООП

**Наскільки зрозумілою була взаємодія між бібліотекою та власним кодом?**
Взаємодія загалом зрозуміла, але має свої особливості. Для використання можливостей рушія (наприклад, фізики) власні класи мають успадковуватися від класів Phaser (наприклад, `class Coin extends Phaser.Physics.Arcade.Sprite`). Це створює сильну зв'язність (tight coupling) між власним кодом та бібліотекою. Необхідно чітко розуміти життєвий цикл об'єктів Phaser, наприклад, що об'єкт потрібно не тільки створити через `new`, але й додати до сцени (`scene.add.existing(this)`) та активувати фізику (`scene.physics.add.existing(this)`).

---

### 4. Проблеми та негативні аспекти (Юніт-тестування)

**Чи виникали проблеми? Що поганого можна сказати про бібліотеку?**
Найскладнішим аспектом роботи стало виконання вимоги щодо **модульного тестування (unit testing)**. Phaser архітектурно спроєктований як монолітний рушій, який важко тестувати частинами поза межами браузерного середовища із запущеним ігровим циклом.

При спробі написати юніт-тест для методу `interact()` класу `Coin`, який має робити монетку невидимою (`this.setVisible(false)`), ми зіткнулися з тим, що рушій намагається виконати багато внутрішньої роботи: оновити списки відображення, пересортувати глибину рендерингу (`queueDepthSort`), випромінити внутрішні події.

У ізольованому тестовому середовищі цих систем не існує. Спроба створити "моки" (імітації) сцени та її систем перетворилася на написання великої кількості шаблонного коду для заглушок, який був складнішим за код, що тестувався. Це свідчить про те, що архітектура Phaser не сприяє легкому написанню класичних юніт-тестів для ігрових об'єктів.

---

### 5. Позитивні аспекти

**Що хорошого можна сказати про цю бібліотеку?**
* **Arcade Physics:** Вбудована фізика — це головна перевага. Налаштування гравітації, платформ, стрибків та збору предметів зайняло мінімум часу та коду. Реалізація цього з нуля була б значно складнішою.
* **Система подій (Events):** Phaser має потужну вбудовану систему подій (`scene.events`). Використання її для комунікації між ігровими об'єктами (наприклад, між підбиранням монети та оновленням таблиці рахунку) дозволило значно зменшити зв'язність коду та покращити архітектуру.

---

### 6. Висновки та ретроспектива

**Що варто було б змінити, враховуючи отриманий досвід?**
Якби довелося починати роботу заново, я б змінив стратегію проєктування та тестування:

1.  **Архітектурний поділ:** Замість того, щоб писати всю логіку всередині класів, що розширюють спрайти Phaser, варто максимально виносити "чисту" ігрову логіку (стан гри, підрахунок очок, інвентар) в окремі, незалежні від Phaser класи. Такі класи дуже легко покривати юніт-тестами.
2.  **Стратегія тестування:** Для об'єктів, які є частиною рушія (візуальні спрайти), замість спроб емулювати весь рушій в юніт-тестах, доцільніше використовувати структурні тести (перевіряти наявність методів/властивостей) або покладатися на інтеграційні тести. Боротьба з рушієм у юніт-тестах виявилася непродуктивною.

---
**Використані ресурси:**
* [Phaser 3 API Documentation](https://newdocs.phaser.io/) — основне джерело інформації про класи та методи.
* [Phaser 3 Examples Labs](https://labs.phaser.io/) — ключовий ресурс для розуміння практичної реалізації.